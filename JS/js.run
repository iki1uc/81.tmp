export function Station(atom) {

    const Z = Z_Axis(atom);
    const V = V_Axis(atom);
    const T = Triangle(atom);
    const O = OrbitSync(atom);

    return {
        W: W(Z + V + T + O),
        S: S(Z + V + T + O),
        A: A(Z + V + T + O),
        D: D(Z + V + T + O)
    };
}

